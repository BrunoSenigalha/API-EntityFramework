// appsettings.Development.json é o arquivo utilizado no desenvolvimento
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  //ConnectionStrings é a conexão com o banco de dados sendo criada.
  // Server= Digitar o caminho do servidor local
  // Initial Catalog= Digitar o nome do banco de dados que será criado
  // Integrated Security= Significa que iniciará com a autenticação local, do windows
  "ConnectionStrings": {
    "ConexaoPadrao": "Server=localhost\\sqlexpress; Initial Catalog=Agenda; Integrated Security=True; TrustServerCertificate=True"
  }
}
